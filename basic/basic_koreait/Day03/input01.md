# input01

```python
# print() 출력 ==> 모니터
# input() 입력 ==> 키보드

# 자료형 ==> 1. 숫자, 2. 문자

print("10 + 10")  # 문자 ==> 그림과 같이 그냥 모양대로 그려준다
print(10 + 10)  # 숫자 ==> 산술연산이 가능하다

test01 = input() # 엔터를 누를때까지 기다리다가 엔터앞에 내용을 변수에 저장한다
print(test01)
print("종료")

# (문제점) 입력된 데이타는 문자로 저장이 된다.
# 캐스팅 (형변환) 원하는 형태로 자료형을 바꿔준다.
# 방법 ==> int(문자)

```

