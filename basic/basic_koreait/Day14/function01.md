# function01

```python
# 변수의 생명주기

# 전역 변수 ==> 프로그램이 종료될때 메모리에서 해제
# 지역 변수 ==> 함수 내부 ==> 함수가 종료할때 메모리에서 해제

# 함수정의
# def 함수이름(매개변수):

# 함수호출
# 함수이름(인자)
i = 20
def test(): # 설계도
    ii = 10
    print("함수내부: ", ii)
    return ii

print(i) # 20
test() # 함수호출시 메모리에 올라간다.
print(i) # 20
i = test() # ==> i ==> 10 # 종료되기 직전 잠깐 살아있을때 저장
print(i) # 10

#def test1(num):
# 메모리 종류 3개
# 1. 스택 ==> 함수
# 2. 힙
# 3. 데이타(전역) ==>
# 프로그램이 종료될때 메모리에서 해제(제일 왼쪽에 붙여서 만든 변수들)
# c c++ +=> java == c#
# python
#lst = [10,20, 30, 40, 50]
#lst = []

```

