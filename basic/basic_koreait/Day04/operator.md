# operator

```python
# 1. 저장 (자료구조) ==> 1. 변수, 2. 리스트
# 2. 수정 (알고리즘) ==> 1. 연산자(4종류) 2. 조건문 3. 반복문

# 1. 산술연산자  + - * / // %
# 2. 대입연산자  a = b (복사) b의 값을 a 에 저장
# 3. 비교연산자  == , != , > , < , >= , <=
"""
num1 = 10
num2 = 20
print(num1 == num2)  # False ==> == 같다.
num2 = 10
print(num1 == num2) # True
"""
"""
# 문제 1 ) 0~100 사이의 숫자(성적을 입력받고 60점 이상이면 True 출력 이하이면 False를 출력
score1 = input("성적을 입력하시오 >>")
# 캐스팅 ==> 문자를 숫자로 바꾼다
# 캐스팅 방법 ==> int("문자")
# 우리가 키보드로 입력받으면 문자로 저장되기 때문에 캐스팅을 해서 숫자로 바꿔줘야 연산이 가능하다.
print(int(score1) >= 60)  # 크거나 같다.
"""
# 문제 2) 숫자 2개를 입력받고 두 숫자가 같으면 True 틀리면 False 출력하세요
num3 = input("숫자1을 입력하세요>>")
num4 = input("숫자2를 입력하세요>> ")
print(int(num3) == int(num4))
```

