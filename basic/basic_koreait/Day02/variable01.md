# variable01

```python
# 1. 저장  ==> 1. 변수 2. 리스트(배열) 3. 클래스
# 2. 찾기  ==> 1. 연산자(4종류) 2. 조건문(if) 3. 반복문(while)

# 변수 (variable) : 데이터를 담을 수 있는 공간
# 변수의 규칙 (특징)
#  1) 이름은 숫자로 시작할 수 없다.  1num = 20 (x)
#  2) 특수문자는 _(언더바)를 사용할 수 있다. num_num = 20  , _num = 30
#  3) 값을 한가지만 저장 가능하다.  ==> 새로운 값이 들어오면 이전의 값은 지워진다.(중요!!!!!!!!!!)

# 산술연산자( + - * / ) , 대입연산자( = )
# 대입연산자 ==> a = b  ==> b 의 값을 a 에 저장한다.
# num ==> 변수 num = 10
num = 10
print(num)  # 10
num = -10
print(num) # -10
num = 1000 # 새로운 값이 들어오면 이전의 값은 지워진다.(!!!!)
print(num) # 1000
num_num = 20
_num = 30
num1 = 10
num2 =3
print(10, "+", 3, "=", 10+3)
# 문제 1) 숫자 자리에 전부 변수를 사용해서 출력 해보세요 - * / // %
print(num1, "-", num2, "=", num1-num2)
print(num1, "*", num2, "=", num1*num2)
print(num1, "/", num2, "=", num1/num2)
print(num1, "//", num2, "=", num1//num2)
print(num1, "%", num2, "=", num1%num2)
```

